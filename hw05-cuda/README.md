# HW 5 — CUDA Mandelbrot Optimization

Four CUDA kernel variants that progressively optimize Mandelbrot generation.

## Techniques explored

- Kernel launch and memory-layout tuning
- Instruction-level parallelism
- Loop unrolling
- Early-exit checks and reduced control overhead

The submitted kernels are under `src/`. The course host program and benchmark harness are not redistributed.
